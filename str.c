#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * man - strtok
 * @arguments: argv[0] argv
 * Return: Always 0
 */
int main(void)
{
	char **split(char *str)
	
		char *argv[];
		int i = 0;


		char *argv[] = strtok{str, "/bin/ls"};
		while (argv[] != NULL)
}
int main(void(int argv[], char **argv))
{
	char *argv[] = {"/bin/ls", "-l", "/usr/", NULL};
	char *split = NULL;

	size_t size_split = 0;
	int argv[]_size = 0;

	char **argv[] = NULL;
	char *argv = NULL;

	printf("Before string\n");
	for int i = 0;
	if (split(argv[], argv, NULL) == -1)
		perror("strtok");
	printf("After string\n");
	return 0;
}
